{
  "$schema": "https://schema.railpack.com",
  "build": { "installCommands": ["pip install -r requirements.txt"] },
  "deploy": {
    "startCommand": "PYTHONPATH=src python -m bridge.cli run --live --create-missing"
  }
}
